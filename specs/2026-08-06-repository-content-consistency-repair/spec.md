# Spec：修复全仓自然语言事实漂移与术语不一致

## 基本信息

- 事项 ID：`2026-08-06-repository-content-consistency-repair`
- 创建日期：`2026-08-06`
- 事项状态、关系和影响范围以 [meta.yaml](meta.yaml) 为唯一事实来源。

## 输入来源

| 类型 | 引用或摘要 | 版本/日期 | 适用范围 |
| --- | --- | --- | --- |
| 用户输入 | 使用 `refactor-natural-language-content` 深入审计全仓，并按审计建议顺序完成修复 | 2026-08-06 | 全仓自然语言工程内容 |
| 仓库证据 | 三个 Archived Meta、当前 Framework/Skill/Eval 目录、Harness 检查与 81 项测试 | 当前工作树 | 事实漂移、术语和权威来源 |

## 背景与目标

全仓结构、链接、Schema、Eval Replay 和程序测试均通过，但自然语内容存在权威来源重复、当前事实漂移、阶段叙事滞后和局部术语不一致。本事项保留已封存历史，修复仍具规范效力的 Skill、模板、能力说明、Blueprint 与 Knowledge。

## 非目标

- 不改写已有 Archive Receipt、Lifecycle Event 或已封存的历史 Spec/Tasks。
- 不修改普通业务代码、测试逻辑或扩展确定性 Validator 能力。
- 不因统一文风重写未发现事实或契约问题的章节。
- 不执行 Commit、Push、PR/MR 或归档。

## 用户或调用场景

1. Agent 从 Specflow 模板建立新事项时，不会在 Spec 正文中创建第二套状态、关系或影响范围。
2. 维护者阅读能力地图和能力说明时，能区分当前已实现子集与未提供的完整能力。
3. 采用方阅读组件治理和 Skill Eval 说明时，不会把已存在的 Validator/Replay 证据误解为未来产物或完整能力。

## 输出与行为契约

- 当前事实以仓库目录、Meta、可运行实现、测试和 Eval Evidence 为依据。
- 模板和当前说明只引用唯一权威状态，不复制动态事实。
- 已实现子集与未实现完整能力分层表达，不用“首期”或“未来”掩盖当前事实。
- 历史不一致只作为已知证据保留，不绕过不可变历史边界。

## 完成条件

- [x] **AC-001** Specflow 模板不再复制 Meta 拥有的状态、关系和影响范围，并指向唯一事实来源。
- [x] **AC-002** Framework 清单、Active Spec 证据和 Web/Evidence 成熟度与当前仓库实现一致。
- [x] **AC-003** 组件 Validator 的已实现子集与生产化缺口表达一致。
- [x] **AC-004** Skill Eval 资源按当前证据形态准确分类，不使用失效的固定总数叙事。
- [x] **AC-005** `data_element`、组件 Registry 和中文说明在相关模板与示例中统一。
- [x] **AC-006** 链接、Schema、Specflow、Knowledge、Distribution、Eval 和全部自动化测试通过；本事项变更在 Scope 内可识别，且没有覆盖并行事项的已有内容。

## 约束

- 技术约束：结构与内容检查使用当前 Harness，不为本次文档修复新增程序。
- 兼容约束：保留现有文件路径、标题入口和公开契约键。
- 权限与安全约束：不改写历史封存产物，不执行未授权的 Git 或外部操作。
- 数据与隐私约束：不引入真实内部实例、账号、域名或稳定个人标识。

## 风险、假设与待确认项

| 类型 | 内容 | 影响 | 处理方式 | 状态 |
| --- | --- | --- | --- | --- |
| Risk | “子集已实现”被误写为“完整能力已实现” | 夸大成熟度 | 逐项对照实现、测试和生产化缺口 | resolved |
| Risk | 修改 Skill 内容使 Distribution 摘要失效 | 仓库检查失败 | 在内容稳定后用同一确定性摘要算法刷新 Manifest | resolved |
| Assumption | 历史 Archived 产物保留当时文本 | 历史内部不一致仍可见 | 在当前模板和规范中防止复发，验证报告记录边界 | accepted |

## Section Index

| 章节 | 说明 | 何时需要读取 |
| --- | --- | --- |
| 背景与目标 | 审计结论和修复边界 | 理解修复原因时 |
| 完成条件 | 六类修复的验收边界 | 实施和验证时 |
| 风险、假设与待确认项 | 历史不可变和成熟度边界 | 处理边界冲突时 |
