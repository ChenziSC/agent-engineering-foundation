# 交付形态与成熟度

## 交付形态

| 形态 | 主要内容 | 是否必须可运行 |
| --- | --- | --- |
| 设计框架 | 问题模型、对象、边界、状态、决策规则和失败模式 | 否 |
| Blueprint | 数据流、契约、扩展点、安全边界和生产化缺口 | 否 |
| Template / Asset | 可复制修改的 Markdown、YAML 或 JSON 骨架 | 否 |
| 可使用 Skill | `SKILL.md`、References、Assets 和行为案例 | Agent 必须能按说明执行，不要求附带代码 |
| Starter | 可复制或初始化的最小项目骨架 | 是 |
| 参考实现 | Package、CLI、Adapter、Script 和 Test | 是 |

Markdown-first 表示设计原因和契约是一等产物，不表示仓库永远不提供代码。确定性实现只有在可以降低错误、稳定复用或形成机器门禁时才增加。

## 成熟度标记

| 标记 | 含义 |
| --- | --- |
| `idea` | 已确认问题价值，但边界和交付尚未完整设计 |
| `designed` | 问题、契约、职责、安全边界和合成案例已经明确 |
| `usable` | Skill、模板或 Starter 可以被其他项目实际采用 |
| `reference-implemented` | 已提供有测试的可运行参考实现 |
| `validated` | 已通过与产物相匹配的行为回放或程序测试 |

成熟度描述具体资产，不自动向整个能力传播。例如 Specflow Skill 可以是 `usable`，归档契约是 `designed`，未来 Archive Validator 仍是 `idea`。

## 当前落地状态

| 能力或资产 | 当前状态 | 真实证据 | 尚未提供 |
| --- | --- | --- | --- |
| 仓库自举 Specflow | `usable` | `specs/`、当前 Active Spec、根 `AGENTS.md` | 确定性 Context 和终态执行器 |
| 长期 Knowledge 治理 | `designed` | `knowledge/`、Registry、Code Entry Map 和当前条目 | Registry/新鲜度 Validator |
| Specflow Skill | `usable` | Skill、Blueprint、模板、12 个行为案例和 Rubric | 正式行为回放结果 |
| Specflow 归档治理 | `designed` | Receipt/Event/Projection Reference、模板、JSON Schema 和 Case 07～12 | 摘要计算、不可变写入和校验器 |
| Skill 运行时与分发 | Blueprint `designed`；项目级子集 `reference-implemented` | Blueprint、模板、CLI、内容摘要、项目级安装/更新、Adapter Registry 和开放 Host | 分发 Manifest 执行、用户级安装和动态插件分发 |
| 项目基建 Adapter | 公共契约 `designed`；Host 注册子集 `reference-implemented` | Integration Manifest、显式 Registry、状态/凭证边界和合成 Host 测试 | 各能力的真实 Adapter 与认证实现 |
| AI 友好仓库 | 模板 `usable`；仓库检查 `reference-implemented` | 根/模块规则、Knowledge、Review 模板、最小 Starter、仓库级只读检查和 CI | Registry 语义与 Knowledge 新鲜度检查 |
| Evidence 与 Claim | `designed` | 框架和 Schema 示例 | 通用参考实现 |
| 长任务 Checkpoint | `designed` | 框架和 Schema 示例 | 通用参考实现 |
| Skill 行为评估 | `usable` | 方法框架、Case/Rubric/Report 模板 | 通用 Runner 和 Scorer |
| Web 首屏预请求 | `validated` | Framework、Skill 和正式行为回放报告 | Validator、Mock Adapter 和合成 Demo |
| Web 性能评审 | `validated` | Skill、报告模板和正式行为回放报告 | Trace/HAR Parser 和 Browser Adapter |
| 项目组件治理与维护 | `usable` | Framework、Skill、Template、Blueprint、Case 和 Rubric | 正式行为回放、Registry Validator |
| Starter / Harness / Doctor | 最小范围 `reference-implemented` | Starter、Node.js CLI、开放 Host、可注入 Registry、仓库检查、CI 和 12 个自动化测试 | Archive Validator、Knowledge 新鲜度和用户级 Host |

当前参考实现只证明项目级最小闭环；不能因为该 CLI 通过测试，就声称 Archive、Knowledge 新鲜度、全仓 Schema、所有 Host 或正式发布已经具备自动校验能力。

## 成熟度门禁

### 升级为 `designed`

- 问题、非目标、输入输出和对象所有权明确；
- Agent、程序和人工职责分开；
- 安全边界、失败模式和合成案例完整；
- 公开等级和独立重建依据已记录。

### 升级为 `usable`

- Skill 或模板有明确触发、步骤和输出；
- 链接、YAML/JSON 和目录结构通过检查；
- 使用方不依赖受限平台即可执行核心流程；
- 未实现的程序能力不会被写成必然存在。

### 升级为 `reference-implemented`

- 可运行代码和公开接口实际存在；
- 失败不会留下不一致或半写入状态；
- 有单元或端到端测试；
- 版本、依赖和兼容边界明确。

### 升级为 `validated`

- Skill 有正式行为回放及保存的证据；或
- 确定性实现通过与风险匹配的自动化测试；
- 验证使用的模型和推理强度由调用时环境选择，不写死在仓库契约中；
- 验证范围和未覆盖内容保持可见。

## 工程化判断

满足以下任一条件时，才把设计资产升级为实现：

- 人工步骤频繁出错，程序可以显著降低风险；
- 需要机器校验稳定 Schema、状态机或安全门禁；
- 已出现多个使用方需要同一确定性能力；
- 实现本身能形成跨项目的最小纵向闭环。

不要为了目录完整创建空 Package、Script、Test 或 Adapter。
