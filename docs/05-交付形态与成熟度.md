# 交付形态与成熟度

## 交付形态

| 形态 | 主要内容 | 是否必须可运行 |
| --- | --- | --- |
| 设计框架 | 问题模型、对象、边界、状态、决策规则和失败模式 | 否 |
| Blueprint | 数据流、契约、扩展点、安全边界和生产化缺口 | 否 |
| Template / Asset | 可复制修改的 Markdown、YAML 或 JSON 骨架 | 否 |
| 可使用 Skill | `SKILL.md`、References、Assets 和行为案例 | Agent 必须能按说明执行，不要求附带代码 |
| Starter | 可复制或初始化的最小项目骨架 | 是 |
| 参考实现 | Package、CLI、Adapter、Script 和 Test | 是 |

Markdown-first 表示设计原因和契约是一等产物，不表示仓库永远不提供代码。确定性实现只有在可以降低错误、稳定复用或形成机器门禁时才增加。

## 成熟度标记

| 标记 | 含义 |
| --- | --- |
| `idea` | 已确认问题价值，但边界和交付尚未完整设计 |
| `designed` | 问题、契约、职责、安全边界和合成案例已经明确 |
| `usable` | Skill、模板或 Starter 可以被其他项目实际采用 |
| `reference-implemented` | 已提供有测试的可运行参考实现 |
| `validated` | 已通过与产物相匹配的行为回放或程序测试 |

成熟度描述具体资产，不自动向整个能力传播。例如 Specflow Skill 可以是 `usable`，本地双终态事项关系事务是 `reference-implemented`，Active/多事项/跨仓库关系事务仍是 `designed`。

## 当前落地状态

| 能力或资产 | 当前状态 | 真实证据 | 尚未提供 |
| --- | --- | --- | --- |
| 仓库自举 Specflow | `usable`；Meta/Context、双终态事项 Relation Transaction 与 Change Gate 子集 `reference-implemented` | `specs/`、当前 Active Spec、完整 Meta Schema、`specflow check`、`context resolve`、`finalize-relation` 和 `change gate check` | Active/多事项/跨仓库关系事务 |
| 长期 Knowledge 治理 | 文档规则 `usable`；检查与 Registry 投影子集 `reference-implemented` | `knowledge/`、Registry、Code Entry Map、来源摘要、`knowledge check`、Projection Plan/Apply/Verify 和 Doctor | Knowledge 正文生成与刷新触发语义判断 |
| Specflow Skill | `validated` | Skill、Blueprint、模板、12 个行为案例、脱敏 Trace 和可重放评分配置 | 真实项目长期采用数据 |
| Specflow 归档治理 | 本地生命周期、双终态事项关系事务、Git 摘要与 Change Gate 子集 `reference-implemented`；Active/多事项/跨仓库事务 `designed` | Receipt/Event/Relation Transaction/Projection/Change Gate Reference、模板、JSON Schema、Finalize/Verify 脚本、本地 Git Merge Candidate Adapter、工作/交付门禁和合成测试 | 其他版本控制 Provider、Active/多事项/跨仓库关系事务、受保护历史检查和外部交付平台集成 |
| Skill 运行时与分发 | Blueprint `designed`；项目级与 Distribution Manifest 子集 `reference-implemented` | Blueprint、白名单 Manifest、Plan/Apply/Verify、内容摘要、项目级安装/更新、Adapter Registry 和开放 Host | 用户级安装和动态插件分发 |
| 项目基建 Adapter | 公共契约 `designed`；Host 与本地 Git 子集 `reference-implemented` | Integration Manifest、显式 Registry、开放 Host、本地 Git Merge Candidate 摘要和合成测试 | 其他能力的真实 Adapter 与认证实现 |
| AI 友好仓库 | 模板 `usable`；仓库检查 `reference-implemented` | 根/模块规则、Knowledge、Review 模板、最小 Starter、Meta/Context 校验、Section Index、祖先规则导航、规则体量/结构矛盾/精确重复检查和 CI | 自然语言规则冲突判断与更多语言入口分析 |
| Evidence 与 Claim | `reference-implemented` | v1 Schema、模板、完整性封存、引用/状态/双向 Blocker 校验和测试 | Evidence 语义真实性自动判断 |
| 长任务 Checkpoint | `reference-implemented` | v1 Schema、模板、摘要封存、连续 Event 校验、恢复计划和测试 | 采用方持久化、锁和外部动作确认 Adapter |
| Skill 行为评估 | `reference-implemented` | 方法框架、Schema、模板、动态 Case Runner、阻塞优先 Scorer、比较器和测试 | 宿主原始 Trace 归一化与 Meta Eval |
| 自然语言工程内容一致性重构 | `validated` | Skill、分层扫描 Reference、7 个行为案例、脱敏 Trace 和可重放评分配置 | 真实 Token/时间成本数据 |
| 增量覆盖与安全变更 | Validator `reference-implemented`；Skill `usable` | 路径覆盖矩阵、自动检查、浏览器场景、人工门禁、模板、行为案例和测试 | Coverage/Browser 真实采集 Adapter |
| Web 首屏预请求 | Skill `validated`；Evidence/资格子集 `reference-implemented` | Framework、Skill、正式行为回放、HAR/Trace Observation 与候选资格测试 | Browser Adapter、真实多次采集与合成应用 Demo |
| Web 性能评审 | Skill `validated`；Evidence Parser 子集 `reference-implemented` | Skill、报告模板、正式行为回放和共享 HAR/Trace Parser | Browser Adapter、Source Map 与真实采集器 |
| Design-to-Code | Skill `usable`；Contract `reference-implemented` | Framework、Skill、Schema、模板、行为案例和测试 | 设计 Provider、视觉差异与浏览器 Adapter |
| 埋点治理 | Skill `usable`；Event Catalog `reference-implemented` | Framework、Skill、Schema、模板、行为案例和测试 | SDK 代码投影、数据平台 Schema 发布与实测 Adapter |
| 项目组件治理与维护 | `validated`；Registry/JS-TS 静态分析子集 `reference-implemented` | Framework、Skill、Template、Blueprint、7 个 Case、脱敏 Trace、可重放评分配置、Registry、静态导出/消费者/兼容基线测试 | 组件抽象价值判断、完整编译器语义和其他语言 Parser |
| Starter / Harness / Doctor | 最小范围 `reference-implemented` | Starter、Node.js CLI、开放 Host、可注入 Registry、只读接入计划、Meta/Context、Knowledge Projection、本地 Git 摘要、Change Gate、Distribution、Eval、组件 Registry、工作区与 Git 对象扫描和 CI | 自然语言规则冲突判断、Knowledge 语义生成和用户级 Host |

当前参考实现只证明项目级最小闭环和本地双终态事项关系事务；不能因为该 CLI 通过测试，就声称 Active/多事项/跨仓库事务、Knowledge 语义复核、全仓 Schema、所有 Host 或正式发布已经具备自动校验能力。

## 成熟度门禁

### 升级为 `designed`

- 问题、非目标、输入输出和对象所有权明确；
- Agent、程序和人工职责分开；
- 安全边界、失败模式和合成案例完整；
- 公开等级、通用性、内部信息移除情况和具体表达/实现的复用依据已记录。

### 升级为 `usable`

- Skill 或模板有明确触发、步骤和输出；
- 链接、YAML/JSON 和目录结构通过检查；
- 使用方不依赖受限平台即可执行核心流程；
- 未实现的程序能力不会被写成必然存在。

### 升级为 `reference-implemented`

- 可运行代码和公开接口实际存在；
- 失败不会留下不一致或半写入状态；
- 有单元或端到端测试；
- 版本、依赖和兼容边界明确。

### 升级为 `validated`

- Skill 有正式行为回放及保存的证据；或
- 确定性实现通过与风险匹配的自动化测试；
- 验证使用的模型和推理强度由调用时环境选择，不写死在仓库契约中；
- 验证范围和未覆盖内容保持可见。

## 工程化判断

满足以下任一条件时，才把设计资产升级为实现：

- 人工步骤频繁出错，程序可以显著降低风险；
- 需要机器校验稳定 Schema、状态机或安全门禁；
- 已出现多个使用方需要同一确定性能力；
- 实现本身能形成跨项目的最小纵向闭环。

不要为了目录完整创建空 Package、Script、Test 或 Adapter。
