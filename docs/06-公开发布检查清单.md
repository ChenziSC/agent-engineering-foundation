# 公开发布检查清单

这份清单用于发布前收口，不替代法律意见、公司审批或代码托管平台的安全能力。

## 仓库边界

- [x] GitHub 仓库名称改为 `agent-engineering-foundation`。
- [x] 本地 `origin` 更新为改名后的地址。
- [x] README 中的名称、定位和实际仓库内容一致。
- [x] 宠物资源可以保留，但与 Agent 工程能力保持依赖隔离。

持续约束：不要使用旧仓库名创建新的 GitHub 仓库，以免旧地址重定向失效。

本地目录名称不影响 Git 仓库身份；当前为了辨识度同步使用 `agent-engineering-foundation/`。

## 权属与来源

- [x] 私有覆盖台账记录了来源区域、公开目标、独立重写方式和排除项。
- [x] Agent 工程内容按独立重写流程形成，没有以内部正文或代码为底稿逐句修改。
- [x] Agent 工程内容中的公开案例、URL、接口和数据均为自行构造。
- [x] 仓库维护者确认宠物图片、名称和说明可随仓库公开分发。
- [x] Agent 工程内容的公开来源和第三方内容情况已单独说明。
- [x] 宠物资源标记为维护者自制，并采用仓库统一许可证。
- [ ] 完成适用的内部审批或权属确认。

## License

- [x] 为仓库选择与发布意图一致的开源许可证。
- [x] Apache License 2.0 覆盖 Markdown、模板、Schema、Skill 和图片资源。
- [x] 当前没有需要保留的第三方归属声明，因此不创建空 `NOTICE`。

许可证只能说明他人如何使用本仓库，不能证明发布者拥有内部材料或第三方资源的权利。

## 内容检查

- [x] 扫描当前工作区中的公司、平台、仓库、服务、员工和内部包名称。
- [x] 扫描当前工作区中的内部域名、接口、应用 ID、环境、Region、绝对路径和邮箱。
- [x] 扫描当前工作区中的 Token、Cookie、Header、密钥、证书和权限配置。
- [x] 确认 Agent 工程内容没有生产 Trace、HAR、截图、真实 URL、业务数据或内部故障码。
- [x] 扫描 Git 历史文件内容、提交消息和已删除文件。
- [x] 将现有提交的作者和提交者改为 GitHub 账号及其公开邮箱。
- [x] 提交前对最终暂存快照重新执行上述扫描。
- [x] 人工复核当前扫描命中项，没有把“扫描无结果”等同于权属确认。

## 质量检查

- [x] 两个 Skill 通过结构校验。
- [x] YAML、JSON、JSON Schema、Markdown 链接和表格通过静态校验。
- [x] 回放 `web-first-screen-prefetch` 的五个行为案例并保存评分结果。
- [x] 回放 `web-performance-review` 的五个行为案例并保存评分结果。
- [x] 确认 `validated`、`usable`、`designed` 和未来可选工程化没有混淆。

## GitHub 操作

- [x] 执行仓库改名。
- [x] 更新本地 `origin` 并确认 `git fetch` 正常。
- [x] 在最终差异审阅后再提交。
- [x] 推送前再次运行工作区和 Git 历史扫描。
- [x] 检查 GitHub 仓库描述、可见性和默认分支。
- [x] 当前未启用 GitHub Pages；未来启用时需单独检查页面内容和链接。

仓库改名、提交和推送都是独立动作。完成其中一项，不代表其他动作已经获得授权。
