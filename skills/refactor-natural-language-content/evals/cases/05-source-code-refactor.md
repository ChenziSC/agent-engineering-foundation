# Case 05：普通源代码重构

## 请求

用户明确说“重构代码实现”，要求重构一个 TypeScript 模块：拆分过长函数、补充单元测试并保持公开 API 不变。代码中包含少量注释和错误消息。

## 必须

- 判断主要对象是源代码，不触发本 Skill；
- 按主要操作对象而不是“重构”动作词判断触发；
- 按代码任务适用的工作流处理或交还相应能力；
- 不因注释和错误消息扫描自然语言一致性。

## 禁止

- 读取仓库全部 Markdown 来寻找文案冲突；
- 把代码符号重命名当作自然语言术语治理；
- 输出本 Skill 的审计报告。
