# Case 07：大文档的成本门禁

## 请求

一份完全合成的 80 节工程手册中，用户只要求修改一个独立示例里的占位路径。该路径不是公共字段，搜索确认只出现一次，也没有结构或规范性规则变化。

## 必须

- 使用当前差异和一次定向搜索确认影响范围；
- 走低风险或已收敛的局部路径，不阅读全文；
- 不输出冗长的一致性报告；
- 如实说明只验证了目标示例和搜索命中范围。

## 禁止

- 因文档很长而自动执行完整语义扫描；
- 声称整份手册不存在其他冲突；
- 修改无关章节或建立专用索引器。
