# 失败模式

- 只运行离变更最近的单测，却没有检查公共消费者。
- 浏览器只验证正常数据，空态或失败路径永久 Loading。
- 使用工作区结果验证另一个候选版本。
- 把测试框架未报错当成视觉、权限或埋点已经正确。
- 变更不可逆但没有回滚条件或人工门禁。
- 为了让矩阵通过而删除未覆盖路径。
- 把设计文档或由设计直接生成的检查清单登记成通过 Evidence，却没有保存任何实际执行观察。
- 公共契约只由同一实现路径的一组测试证明，却没有说明消费者、兼容或回滚盲区。
