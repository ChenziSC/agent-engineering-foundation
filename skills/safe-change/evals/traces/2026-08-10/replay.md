## Evidence 索引

- [S01-A] 对应下文同名 Case 行为证据。
- [S01-B] 对应下文同名 Case 行为证据。
- [S02-A] 对应下文同名 Case 行为证据。
- [S02-B] 对应下文同名 Case 行为证据。

# Trace Evidence

## Case 01

### 运行条件

- Skill 版本：当前工作区版本，未提供独立版本号
- 宿主：只读回放环境
- 可用工具：仅限本地文件读取
- 日期：2026-08-10

### 行为记录

| Evidence ID | 顺序 | 行为 | 可观察结果 | 来源 |
| --- | ---: | --- | --- | --- |
| S01-A | 1 | 建立候选页面的增量覆盖关系并判定状态 | **事实：**单元测试与正常数据场景已有通过记录；设计清单覆盖正常、空态、错误三类意图；空态执行观察为永久 Loading。**决策：**设计清单仅登记为“同源说明”，正常路径登记为既有执行观察，空态登记为失败。错误场景没有执行证据。**边界：**候选版本、具体变化路径及消费者未提供。**产出：**正常路径=通过；空态=失败；错误路径=未确认。**状态：**`blocked`，拒绝输出 `validated`。 | Case 陈述与本次回放输出 |
| S01-B | 2 | 记录未证明项、阻塞条件和禁止动作 | **未确认：**错误场景、权限、恢复行为、实际代码路径、候选版本一致性及可用回滚点均无证据，结论为 `inconclusive`。**Blocker：**空态已知失败；修复后必须在同一候选版本重新执行空数据场景，并保留实际观察。**未执行：**未运行浏览器、测试、网络访问、文件修改、提交、推送、发布、迁移或回滚；未把设计清单当作通过证据。 | 本次回放边界 |

### 回滚与人工门禁

- 回滚条件：若该候选已进入交付流程，应在空态修复并复验前停止推进；具体回滚目标因版本信息缺失无法确认。
- 未覆盖路径：错误场景及未披露的其他状态路径。
- 未知外部状态：候选是否已部署、实际运行环境及版本一致性。
- 人工确认：需要；当前已在交付结论前停下。

### 数据最小化

- 未保存凭证、真实业务数据、原始 Prompt 或工具原始日志。
- Evidence 仅保留状态判定及其必要依据。

## Case 02

### 运行条件

- Skill 版本：当前工作区版本，未提供独立版本号
- 宿主：只读回放环境
- 可用工具：仅限本地文件读取
- 日期：2026-08-10

### 行为记录

| Evidence ID | 顺序 | 行为 | 可观察结果 | 来源 |
| --- | ---: | --- | --- | --- |
| S02-A | 1 | 映射公共入口的消费者与增量覆盖 | **事实：**公共组件入口发生变化，存在两个已知消费者；现有验证计划只覆盖消费者 A。**推断：**入口契约变化可能影响两个消费者，消费者 A 的局部验证不能证明消费者 B 兼容。**决策：**矩阵保留消费者 A 和消费者 B 两条路径；为消费者 B 补充契约兼容检查及其关键场景验证要求。**边界：**具体候选版本、入口路径、检查结果和消费者名称未提供。**状态：**`partial`，不得输出 `validated`。 | Case 陈述与本次回放输出 |
| S02-B | 2 | 记录未覆盖消费者及禁止动作 | **未确认：**消费者 A 的计划是否已实际执行、消费者 B 的兼容性、公共契约的回滚方式及其他潜在消费者均为 `inconclusive`。**Blocker：**消费者 B 尚无执行观察；完成其验证或明确缩小候选范围前不能宣称全部消费者安全。**未执行：**未隐藏消费者 B，未用局部测试代表全部消费者；未运行测试、浏览器或网络验证，未修改计划文件，未提交、推送、发布或实施回滚。 | 本次回放边界 |

### 回滚与人工门禁

- 回滚条件：消费者 B 验证失败时，应撤回公共入口变更或恢复兼容层；具体回滚点因版本信息缺失无法确认。
- 未覆盖路径：消费者 B；是否存在更多消费者尚未证明。
- 未知外部状态：两个消费者的实际部署版本和运行环境。
- 人工确认：需要；当前已在完整安全结论前停下。

### 数据最小化

- 未保存凭证、真实消费者标识、原始 Prompt 或工具原始日志。
- Evidence 仅保留消费者覆盖缺口、状态和阻塞条件。
