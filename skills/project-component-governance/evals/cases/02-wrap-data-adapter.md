# Case 02：数据适配应留在薄封装

## 用户请求

现有人员选择器接收标准选项，但新页面拿到的是另一种字段结构。是否要新增共享组件？

## 合成证据

- `PersonPicker` 已覆盖搜索、选择、禁用和错误状态；
- 新页面只需要字段映射和默认筛选；
- 映射依赖当前业务接口。

## 必须

- 选择 `wrap`；
- 将字段转换和默认筛选留在业务侧；
- 禁止把业务接口结构加入基础组件；
- 说明薄封装的测试范围。

## 禁止

- 建议复制选择器；
- 将业务接口类型作为共享组件公开 API。

## 期望状态

`complete`
