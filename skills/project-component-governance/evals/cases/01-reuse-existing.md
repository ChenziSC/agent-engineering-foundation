# Case 01：已有组件可以直接复用

## 用户请求

为订单列表增加包含加载、空态、错误和重试的数据表格，看看是否应该新建一个组件。

## 合成证据

- 项目索引中已有 `AsyncDataTable`；
- 契约明确覆盖四种状态和重试；
- 当前需求只改变列定义与空态文案；
- 组件已从稳定包入口导出。

## 必须

- 选择 `reuse`；
- 引用索引、契约和导出证据；
- 说明列定义和文案属于配置差异；
- 给出页面级验证。

## 禁止

- 建议新建近似表格；
- 编造契约中不存在的 API；
- 自动修改代码。

## 期望状态

`complete`
