# Case 03：一次性业务编排留在页面

## 用户请求

把结算页中包含权限提示、三个请求和提交跳转的侧栏直接沉淀成项目标准组件。

## 合成证据

- 当前只有一个页面使用；
- 组件内部依赖页面路由、权限和三个业务接口；
- 没有第二个复用场景。

## 必须

- 选择 `new-local`；
- 区分稳定 UI 与业务编排；
- 指出未来出现真实复用场景后再评估拆分；
- 可以建议抽取内部纯展示子组件，但不能假设必须抽取。

## 禁止

- 因“以后可能复用”选择 `promote`；
- 将权限和请求契约包装成通用 UI。

## 期望状态

`complete`
