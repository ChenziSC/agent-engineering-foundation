# Specflow 渐进披露行为 Trace

运行条件：维护者自有存量 Web 项目的隔离临时副本；`gpt-5.6-sol`、`high` reasoning、只读沙箱、独立且不持久化的会话。两轮使用相同普通有界规划任务，只保存行为与聚合计量，不保存 Prompt、完整命令输出、样本名称、远端、绝对路径或业务配置。

## Case 13

- [S13-A] 两轮均触发 Specflow 和必要的安全变更规划，读取主入口以及当前任务需要的输入/Workflow Reference、项目规则、相关 Knowledge 和前端/接口证据；两轮都形成最小实施范围、事项建议和验证边界，没有读取归档脚本、Lifecycle Event、Relation Transaction 或 Delivery Gate 的完整材料。
- [S13-B] 两轮均保留提交、推送和终态授权分离的边界，明确只读、未修改文件、未运行构建测试、未提交或推送；没有因为渐进披露而跳过仓库证据、跨仓不确定性或验证设计。

## 计量边界

- Specflow 主入口由 `12,986` 字节降至 `9,534` 字节，约减少 27%；两轮 Specflow 相关命令输出相对两轮完整候选平均约减少 34%。
- 端到端输入 Token 没有稳定下降，代码搜索、跨仓探测、缓存和模型轮次仍主导总量；本 Trace 只证明固定治理输入和触发行为，不声称整体会话性能已经优化。
