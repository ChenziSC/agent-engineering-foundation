# 模块协作规则：<模块名称>

## 适用范围

- 目录：`<relative/path/>`
- 不适用于：`<相邻但不受此规则约束的目录>`

## 模块职责

用简短文字说明模块负责什么，以及明确不负责什么。

## 稳定入口

| 入口 | 职责 |
| --- | --- |
| `<relative/path/file>` |  |

## 局部不变量

- `<只适用于本模块的稳定规则>`
- `<跨文件必须保持的契约>`

不要重复根规则；只写差异。

## 推荐模式

- `<应优先复用的模式、组件或函数>`

## 禁止模式

- `<已知会导致问题的实现方式>`

## 修改时必须验证

- [ ] `<模块特有的行为>`
- [ ] `<兼容或回归场景>`

## 相关知识

- `<knowledge-entry-id>`

## 何时更新本文件

- 模块职责发生变化；
- 稳定入口移动；
- 局部不变量变化；
- 重复失败证明当前规则不足。

不要因一次性任务进度更新本文件。
