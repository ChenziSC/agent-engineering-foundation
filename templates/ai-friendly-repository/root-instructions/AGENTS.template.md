# 仓库协作规则

## 仓库目标

用一到三句话说明这个仓库解决什么问题，以及最重要的非目标。

## 顶层模块地图

| 目录 | 职责 | 何时需要读取模块规则 |
| --- | --- | --- |
| `src/app/` | 示例应用入口 | 修改路由或初始化时 |
| `src/domain/` | 示例领域逻辑 | 修改核心规则时 |
| `docs/` | 长期文档 | 需要架构和契约背景时 |

只保留稳定入口，不枚举所有文件。

## 全仓不变量

- `<所有模块都必须遵守的规则>`
- `<安全、兼容或数据边界>`
- `<禁止的破坏性操作>`

## 工作方式

- 先读取与目标目录最接近的模块规则。
- 当前任务状态以 Specflow 或等价交付产物为准。
- 长期知识只用于背景，不覆盖当前已确认需求。
- 不把聊天摘要写成仓库事实。
- 不修改任务范围之外的用户内容。

## 验证

- 文档：
- 代码：
- 测试：
- 发布前：

只填写仓库真实存在的命令。没有命令时明确写“人工检查”，不要虚构。

## 外部操作

- 哪些动作需要显式授权：
- 哪些动作禁止自动执行：
- 凭证和敏感数据如何处理：

## 规则发现

- 模块规则命名：
- 长期知识 Registry：
- 当前工作目录：
- 代码入口映射：

## 不应放在这里

- 当前需求进度；
- 单个模块的详细实现；
- 大段架构历史；
- 临时故障记录；
- 可从代码直接发现的细节。
